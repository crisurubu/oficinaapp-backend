services:
  - type: web
    name: oficinaapp-backend
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/seu-app.jar
    autoDeploy: true